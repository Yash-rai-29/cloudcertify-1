The dashboard should be responsive across different screen sizes.

API Specifications:

The dashboard will interact with the following backend APIs:

1. User Information:

Endpoint: GET /b/manage_user/users/me
Request: No request body.
Response (Model):
{
  "first_name": "string",
  "last_name": "string",
  "email": "string",
  "certification_target": "string",
  "uid": "string",
  "full_name": "string",
  "search_name": "string",
  "uuid": "string",
  "created_at": "integer" (timestamp),
  "updated_at": "integer" (timestamp),
  "is_active": "boolean",
  "is_verified": "boolean",
  "avatar_url": "string" (optional, can be null),
  "activity": {
    "last_login": "integer" (timestamp),
    "streak_count": "integer",
    "streak_last_updated": "integer" (timestamp),
    "tests_taken": "integer",
    "questions_answered": "integer",
    "avg_score": "float",
    "study_time_minutes": "integer"
  },
  "preferences": {
    "daily_reminder": "boolean",
    "email_notifications": "boolean",
    "theme": "string",
    "study_goal_minutes_per_day": "integer",
    "target_exam_date": "integer" (timestamp, optional, can be null)
  },
  "subscription": {
    "tier": "string",
    "start_date": "integer" (timestamp),
    "end_date": "integer" (timestamp, optional, can be null),
    "is_active": "boolean",
    "auto_renew": "boolean",
    "payment_method_id": "string" (optional, can be null)
  },
  "completed_modules": "array",
  "saved_resources": "array"
}
2. Daily Streak:

Endpoint: GET /b/user_activity/daily-streak
Request: No request body.
Response (Model):
{
  "current_streak": "integer",
  "last_question_date": "integer" (timestamp)
}
3. Daily Question:

Endpoint: GET /b/user_activity/daily-question?date={date}
Request: date parameter (YYYY-MM-DD format)
Response (Model):
{
  "question": {
    "id": "string",
    "question": "string",
    "options": "array of strings",
    "correct_answer": "string",
    "explanation": "string",
    "date": "integer" (timestamp)
  },
  "user_attempt": {
    "attempted": "boolean",
    "answer": "string" (optional, can be null),
    "is_correct": "boolean" (optional, can be null),
    "timestamp": "integer" (timestamp, optional, can be null)
  }
}
4. User Activities:

Endpoint: GET /b/user_activity/activities
Request: No request body.
Response (Model):
{
  "activities": [
    {
      "id": "string",
      "activity": "string",
      "timestamp": "integer" (timestamp),
      "metadata": "object" (optional, can be null)
    }
  ]
}
5. Test Recommendations:

Endpoint: GET /b/recommendation/recommendations?limit={limit}
Request: limit parameter (integer, specifying the number of recommendations)
Response (Model):
{
  "recommendations": [
    {
      "test_id": "string",
      "title": "string",
      "category": "string",
      "difficulty": "string",
      "recommendation_type": "string",
      "popularity_score": "float"
    }
  ]
}
Dashboard Components: The dashboard should include the sections and elements described in the previous prompt response, using the data from the corresponding APIs.

Error Handling: Implement robust error handling for API requests, including network errors, server errors (e.g., 4xx, 5xx status codes), and empty responses. Display appropriate messages to the user in case of failures. Consider using a loading indicator while fetching data.